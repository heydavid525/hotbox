syntax = "proto3";

package hotbox;

message ExecutionStats {
  repeated TransformStats = 1;
}

message TransStats {
  string hash = 1;
  float t_read_cache = 2;
  float t_transform = 3;
  uint64 n_generated_value = 4;
  float t_write_cache = 5;
}
